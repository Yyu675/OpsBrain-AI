# 统一换行符处理，避免跨平台协作时整文件 diff 噪音。
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd.exe 解析异常。
*.cmd text eol=crlf
*.bat text eol=crlf

# Shell 脚本必须是 LF，CRLF 会导致 "bad interpreter: /bin/sh^M"。
*.sh  text eol=lf
mvnw  text eol=lf

# 二进制资源不做换行符转换。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text
*.jar binary
*.zip binary
*.woff binary
*.woff2 binary
