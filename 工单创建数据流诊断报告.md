# 工单创建数据流诊断报告

> 诊断时间：2026-07-21
> 问题描述：AI Agent 创建的工单未存入数据库

---

## 一、完整数据流链路分析

### 1. 前端发起对话

```
用户输入: "帮我创建一个工单"
  ↓
ChatView.vue - sendMessage()
  ↓
POST /api/v1/agent/chat (SSE 流式响应)
  ↓
AgentController.chat()
```

### 2. 后端 Agent 调度

```
AgentController.chat()
  ↓
ChatApplicationService.executeAgentWithSSE()
  ↓
DevOpsAgentEngine (turboEngine / reasonerEngine)
  ↓
LangChain4j AiServices (ReAct Agent)
  ↓
Agent 决策调用 createDevOpsTicket 工具
```

### 3. 工具执行链路

```
@Tool createDevOpsTicket() 
  → DevOpsTools.createDevOpsTicket()
  ↓
ToolParameterValidator.validateTicketParams() (L3 校验)
  ↓
TicketService.saveTicket()
  ↓
DevOpsTicketRepository.save()
  ↓
JdbcTemplate.update(INSERT INTO sys_devops_ticket...)
  ↓
PostgreSQL 数据库
```

---

## 二、核心代码审查

### ✅ 1. DevOpsTools.createDevOpsTicket() - 工具入口

**位置**: `src/main/java/com/devops/agent/domain/tools/DevOpsTools.java:102-141`

**关键代码**:
```java
@Tool("用户需要开工单/上报二级运维时调用")
public String createDevOpsTicket(...) {
    // L3 Schema 校验
    validator.validateTicketParams(title, priority, module, description);
    
    // 调用工单服务创建工单
    String ticketNo = ticketService.saveTicket(
        title,
        priority.trim().toUpperCase(),
        module.trim().toUpperCase(),
        description,
        null,
        TraceContext.getTraceId()  // ✅ 从 ThreadLocal 获取 traceId
    );
    
    return "✅ 工单创建成功!...";
}
```

**状态**: ✅ **正常** - 已正确调用 `ticketService.saveTicket()`

---

### ✅ 2. TicketService.saveTicket() - 业务逻辑层

**位置**: `src/main/java/com/devops/agent/domain/biz/service/TicketService.java:47-76`

**关键代码**:
```java
public String saveTicket(...) {
    // 生成流水号: TKT-yyyyMMdd-序号
    String ticketId = generateTicketId();
    
    DevOpsTicket ticket = new DevOpsTicket();
    ticket.setId(ticketId);
    ticket.setTitle(title);
    ticket.setPriority(priority);
    ticket.setModule(module);
    ticket.setDescription(description);
    ticket.setStackTrace(stackTrace);
    ticket.setStatus("PENDING");
    ticket.setSourceTraceId(sourceTraceId);  // ✅ traceId 已设置
    ticket.setAssignee("待分配");
    ticket.setCreator("devops-admin");
    ticket.setCategory(mapModuleToCategory(module));
    ticket.setSla(mapPriorityToSla(priority));
    ticket.setCreateTime(LocalDateTime.now());
    ticket.setUpdateTime(LocalDateTime.now());
    
    ticketRepository.save(ticket);  // ✅ 调用 Repository 保存
    
    log.info("🎫 [TicketService] 工单创建成功: {}", ticketId);
    return ticketId;
}
```

**状态**: ✅ **正常** - 实体构建完整，所有字段已设置

---

### ✅ 3. DevOpsTicketRepository.save() - 数据访问层

**位置**: `src/main/java/com/devops/agent/domain/biz/repository/DevOpsTicketRepository.java:32-53`

**关键代码**:
```java
public int save(DevOpsTicket ticket) {
    String sql = """
        INSERT INTO sys_devops_ticket (
            id, title, priority, module, description, stack_trace, 
            status, source_trace_id, assignee, creator, category, 
            sla, create_time, update_time
        )
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        """;
    return jdbcTemplate.update(sql,
        ticket.getId(),
        ticket.getTitle(),
        ticket.getPriority(),
        ticket.getModule(),
        ticket.getDescription(),
        ticket.getStackTrace(),
        ticket.getStatus(),
        ticket.getSourceTraceId(),  // ✅ traceId 已传入
        ticket.getAssignee(),
        ticket.getCreator(),
        ticket.getCategory(),
        ticket.getSla(),
        ticket.getCreateTime(),
        ticket.getUpdateTime()
    );
}
```

**状态**: ✅ **正常** - SQL 字段与实体字段完全对齐，14 个参数全部传入

---

### ✅ 4. 数据库表结构

**位置**: `sql/init.sql:41-56`

**表结构**:
```sql
CREATE TABLE IF NOT EXISTS sys_devops_ticket (
    id              VARCHAR(64) PRIMARY KEY,
    title           VARCHAR(255) NOT NULL,
    priority        VARCHAR(32) NOT NULL,
    module          VARCHAR(64) NOT NULL,
    description     TEXT,
    stack_trace     TEXT,
    status          VARCHAR(32) DEFAULT 'PENDING',
    source_trace_id VARCHAR(64),
    assignee        VARCHAR(64),
    creator         VARCHAR(64) DEFAULT 'devops-admin',
    category        VARCHAR(64),
    sla             VARCHAR(128),
    create_time     TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    update_time     TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**状态**: ✅ **正常** - 表结构与 INSERT 语句完全匹配

---

## 三、潜在问题点排查

### ⚠️ 问题点 1：数据库连接配置

**检查项**: `application-dev.yml` 中的数据库连接配置

**可能原因**:
- PostgreSQL 容器未启动
- 连接地址/端口错误
- 用户名密码错误
- 数据库名称错误

**验证方法**:
```bash
# 1. 检查容器状态
docker ps --filter "name=postgres"

# 2. 尝试连接数据库
docker exec -it <容器名> psql -U devops_user -d devops_ai_db

# 3. 查看工单表数据
SELECT * FROM sys_devops_ticket ORDER BY create_time DESC LIMIT 10;
```

---

### ⚠️ 问题点 2：事务回滚

**检查项**: 是否存在异常导致事务回滚

**可能原因**:
- JdbcTemplate.update() 抛出异常但被捕获
- 外层事务回滚
- 数据库约束冲突（如主键重复）

**验证方法**:
查看后端日志中是否有：
- `SQLException`
- `DataAccessException`
- `constraint violation`
- `duplicate key`

---

### ⚠️ 问题点 3：Redis 序号生成失败

**检查项**: `generateTicketId()` 中的 Redis 连接

**可能原因**:
- Redis 容器未启动
- Redis 连接配置错误
- `redisTemplate.opsForValue().increment()` 返回 null

**验证方法**:
```bash
# 检查 Redis 容器状态
docker ps --filter "name=redis"

# 连接 Redis 查看序号键
docker exec -it <容器名> redis-cli
> KEYS devops:ticket:seq:*
> GET devops:ticket:seq:20260721
```

---

### ⚠️ 问题点 4：日志输出但无实际插入

**检查项**: `JdbcTemplate.update()` 返回值

**可能原因**:
- `jdbcTemplate.update()` 返回 0（未插入任何行）
- 数据库连接池配置问题
- 自动提交关闭（autoCommit=false）

**验证方法**:
在 `DevOpsTicketRepository.save()` 中添加日志：
```java
int rows = jdbcTemplate.update(sql, ...);
log.info("💾 [Repository] 工单入库: ticketId={}, affectedRows={}", ticket.getId(), rows);
if (rows == 0) {
    throw new RuntimeException("工单插入失败，受影响行数为 0");
}
return rows;
```

---

## 四、推荐修复方案

### 方案 A：增强日志追踪（推荐）

**目标**: 在关键节点增加详细日志，精准定位问题

**改动点**:

1. **TicketService.saveTicket()** - 保存前后记录日志
2. **DevOpsTicketRepository.save()** - 记录 SQL 执行结果
3. **application-dev.yml** - 开启 JdbcTemplate SQL 日志

**成本**: 低，10 分钟

**优势**: 
- 不改变业务逻辑
- 精准定位问题环节
- 便于后续调试

---

### 方案 B：添加异常捕获与重试

**目标**: 捕获保存失败的异常，并提供明确错误信息

**改动点**:

1. **TicketService.saveTicket()** - 增加 try-catch
2. **DevOpsTools.createDevOpsTicket()** - 返回失败信息给用户

**成本**: 中，20 分钟

**优势**: 
- 用户能看到明确的错误提示
- 防止静默失败

**劣势**: 
- 需要定义错误码
- 可能影响现有错误处理链

---

### 方案 C：增加数据库健康检查

**目标**: 应用启动时检查数据库连接和表结构

**改动点**:

1. 新增 `DatabaseHealthCheck` 组件
2. 启动时执行 `SELECT 1` 和表结构校验

**成本**: 高，30 分钟

**优势**: 
- 提前发现配置问题
- 确保表结构正确

---

## 五、立即执行的验证步骤

### Step 1: 检查容器状态
```bash
docker ps
```
预期输出：postgres 和 redis 容器都在运行

### Step 2: 验证数据库连接
```bash
docker exec -it <postgres容器名> psql -U devops_user -d devops_ai_db -c "\dt"
```
预期输出：能看到 `sys_devops_ticket` 表

### Step 3: 查询现有工单数据
```bash
docker exec -it <postgres容器名> psql -U devops_user -d devops_ai_db -c "SELECT COUNT(*) FROM sys_devops_ticket;"
```
预期输出：工单总数

### Step 4: 查看后端日志
启动后端应用，发起对话创建工单，观察日志中是否有：
- `[Tool] createDevOpsTicket 被调用`
- `[TicketService] 工单创建成功`
- 是否有任何异常堆栈

---

## 六、结论

**代码层面**：✅ **所有链路正常**
- 工具注册正确
- 业务逻辑完整
- SQL 语句正确
- 表结构匹配

**疑似原因**：⚠️ **环境问题**
- 数据库容器未启动
- 配置文件中的连接信息错误
- 事务回滚但未记录日志

**下一步行动**：
1. 执行上述验证步骤，确认环境状态
2. 选择方案 A（增强日志），精准定位问题
3. 根据日志输出确定最终修复方向
