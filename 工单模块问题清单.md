# 工单模块问题清单与修复方案

## 审查时间
2026-07-17

## 问题统计
共发现 **6 个核心问题**，按优先级排序：

---

## 🔴 P0 - 数据库表结构不一致（阻断性问题）

### 问题1：表结构字段缺失 `stack_trace` 和 `update_time`
**位置**：`sql/init.sql` vs 代码实体

**现状**：
- `init.sql` 表定义中**没有** `stack_trace` 和 `update_time` 字段
- `DevOpsTicket` 实体类有 `stackTrace` 和 `updateTime` 字段
- `DevOpsTicketRepository` 的 SQL 插入语句包含这两个字段
- 运行时必然抛 `PSQLException: column "stack_trace" does not exist`

**影响**：工单创建功能完全无法使用

**修复方案**：在 `init.sql` 的 `sys_devops_ticket` 表中补充字段：
```sql
stack_trace TEXT,                          -- 堆栈信息(可选)
update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP  -- 更新时间
```

---

## 🟠 P1 - 业务逻辑缺陷

### 问题2：`traceId` 传递链路断裂
**位置**：`DevOpsTools.createDevOpsTicket` → `TicketService.saveTicket`

**现状**：
- `DevOpsTools.createDevOpsTicket` 调用 `ticketService.saveTicket()` 时，最后一个参数 `sourceTraceId` 传的是 `null`
- 注释写的是 "由 Service 内部生成"，但 `TicketService` 并没有生成逻辑
- 导致工单与会话关联丢失，看板统计无法追溯

**影响**：工单无法与原始会话关联，丢失溯源能力

**修复方案**：
1. 在 `DevOpsAgentServiceImpl.handleStreamChat` 中把 `traceId` 通过 ThreadLocal/上下文传递给引擎
2. 或在 `DevOpsTools` 中注入 `traceId`（通过 Spring Request Scope Bean）
3. 或在工具调用时通过工具上下文传递 `traceId`

---

### 问题3：`module` 字段硬编码为 "AI-AGENT"
**位置**：`DevOpsTools.createDevOpsTicket:119`

**现状**：
- 工具方法中固定传 `"AI-AGENT"`
- 数据库设计文档定义的枚举值是：`K8S / ALIYUN_SLB / MYSQL / NETWORK`
- 无法区分故障来源模块

**影响**：工单分类失效，无法按模块统计

**修复方案**：
1. **方案A（推荐）**：让 LLM 从用户描述中智能推断模块，在工具参数中新增 `module` 参数
   ```java
   @Tool("用户需要开工单/上报二级运维时调用")
   public String createDevOpsTicket(
       @P("工单标题") String title,
       @P("优先级 [HIGH/MEDIUM/LOW]") String priority,
       @P("故障模块 [K8S/ALIYUN_SLB/MYSQL/NETWORK/OTHER]") String module,
       @P("故障描述与堆栈摘要") String description)
   ```
2. **方案B（保守）**：保持 "AI-AGENT" 作为默认值，新增一个 "AI-AGENT" 到枚举中

---

### 问题4：工单流水号生成算法不可靠
**位置**：`TicketService.generateTicketId`

**现状**：
- 使用 `System.currentTimeMillis() % 10000` 作为序号
- 高并发下可能重复（同一毫秒多次调用）
- 序号不是当日递增，而是随机

**影响**：并发场景可能主键冲突，序号不连续不符合业务语义

**修复方案**：
1. **方案A（推荐）**：使用 Redis `INCR devops:ticket:seq:{yyyyMMdd}` 每日自增
2. **方案B**：使用数据库序列（需额外建表存储每日序号）
3. **方案C（MVP临时）**：改用 UUID 后8位，放弃递增语义但保证唯一性

---

## 🟡 P2 - 代码改进

### 问题5：SQL 拼接存在注入风险
**位置**：`DevOpsTicketRepository.findPage:63-73`

**现状**：
```java
if (priority != null && !priority.isEmpty()) {
    sql.append(" AND priority = '").append(priority).append("'");
}
```
直接拼接参数到 SQL，存在注入风险

**影响**：安全风险（虽然 MVP 无鉴权，但不符合规范）

**修复方案**：改用参数化查询
```java
List<Object> params = new ArrayList<>();
if (priority != null && !priority.isEmpty()) {
    sql.append(" AND priority = ?");
    params.add(priority);
}
// ...
return jdbcTemplate.query(sql.toString(), new TicketRowMapper(), params.toArray());
```

---

### 问题6：工具参数校验与数据库约束不一致
**位置**：`ToolParameterValidator` vs `init.sql`

**现状**：
- 校验器限制 `title.length() > 100` 抛异常
- 数据库字段 `title VARCHAR(255)`，限制 255
- 不一致可能导致困惑

**影响**：边界值不一致，可能浪费重试次数

**修复方案**：对齐边界值，改为 `title.length() > 255`

---

## 架构设计评估

您问到的 **"AI对话 + 短期缓存 + 长期向量库 + RAG索引"** 设计：

### ✅ 合理的部分
1. **语义缓存（Redis，24h TTL）** - 相似度 ≥ 0.95 直接复用，降本显著
2. **向量库（PgVector）** - 父子切片 + 混合检索 + 0.73 阈值熔断，架构清晰
3. **RAG索引（HNSW + GIN）** - 向量余弦 + 全文检索双索引，性能优秀
4. **四层幻觉防护** - Prompt约束 → 工具白名单 → Schema校验 → 相似度熔断，防护到位

### ⚠️ 可优化的部分
1. **会话上下文管理缺失**
   - 当前 `devops:session:{userId}` 键在 Redis 键设计中定义，但代码中未实现
   - 每次对话都是独立的，无法记住上下文（如："那个工单的状态是什么？"无法理解"那个"）
   - **建议**：MVP 可暂时不做多轮上下文（文档已标注"可省略"），但需在产品文档中说明限制

2. **缓存失效策略粗暴**
   - 知识库更新时 `DEL devops:cache:ans:*` 清空全部缓存
   - 可改进为精细化失效：只删除受影响文档相关的缓存
   - **建议**：MVP 可保持当前简单策略，后续迭代优化

3. **缺少分布式锁保护**
   - 工单序号生成、缓存写入等操作在高并发下可能竞争
   - **建议**：MVP 单实例可接受，生产需加 Redis 分布式锁

---

## 修复优先级建议

| 优先级 | 问题编号 | 修复时长估算 | 是否阻断功能 |
|:---:|:---|:---:|:---:|
| 🔴 P0 | 问题1 - 表结构缺字段 | 5 分钟 | ✅ 是 |
| 🟠 P1 | 问题2 - traceId 断链 | 30 分钟 | ⚠️ 部分（无法溯源） |
| 🟠 P1 | 问题3 - module 硬编码 | 20 分钟 | ⚠️ 部分（分类失效） |
| 🟠 P1 | 问题4 - 序号生成不可靠 | 30 分钟 | ⚠️ 部分（高并发风险） |
| 🟡 P2 | 问题5 - SQL 注入风险 | 15 分钟 | ❌ 否（安全加固） |
| 🟡 P2 | 问题6 - 校验边界不一致 | 5 分钟 | ❌ 否（体验优化） |

---

## 修复策略

作为您的开发人员，我将按以下顺序修复：

1. **立即修复 P0 问题1** - 修改 `init.sql` 补充缺失字段
2. **立即修复 P1 问题2** - 实现 `traceId` 传递（通过上下文或方法参数）
3. **立即修复 P1 问题3** - 让 LLM 推断 `module`，新增工具参数
4. **立即修复 P1 问题4** - 改用 Redis INCR 实现可靠序号生成
5. **优化 P2 问题5** - 修改分页查询为参数化
6. **优化 P2 问题6** - 对齐校验边界值

预计总耗时：**1.5 小时**

修复完成后会运行 build 验证，并手动测试工单创建流程。
