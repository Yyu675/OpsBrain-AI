# 工单模块全栈完善方案

## 方案背景

根据您的要求，现需完成以下工作：

1. ✅ **后端业务模块修复** - 已完成（6个问题已修复）
2. 📋 **前端业务模块修复** - 待规划
3. 🔗 **前后端联调测试** - 待执行
4. 📚 **CLAUDE.md 更新** - 从 MVP 阶段切换到完善优化阶段
5. 🚀 **高级特性实现** - 多轮对话、会话上下文管理、缓存优化、分布式锁

---

## 一、前端工单模块现状分析

### 当前实现
- 前端已有完整的工单 UI（`TicketList.vue`、`TicketFormDialog.vue`、`TicketDetail.vue`）
- 前端使用 Pinia store 本地管理工单数据（`stores/tickets.ts`）
- 前端有 mock 数据，支持增删改查、批量操作、状态流转
- 前端已实现前端兜底清单（P0/P1/P2 共14项）

### 存在问题
1. **前端与后端完全脱节**
   - 前端使用本地 mock 数据，未调用后端 API
   - 前端工单结构与后端表结构不一致
   - 前端工单 ID 格式 `OPS-2024-0891` vs 后端 `TKT-20260717-0001`

2. **字段映射不一致**
   | 前端字段 | 后端字段 | 说明 |
   |:---|:---|:---|
   | `id` | `id` | 格式不同 |
   | `title` | `title` | ✅ 一致 |
   | `priority` (urgent/high/medium/low) | `priority` (HIGH/MEDIUM/LOW) | 枚举值不一致 |
   | `status` (pending/processing/resolved/closed) | `status` (PENDING/PROCESSING/CLOSED) | 缺少 resolved |
   | `service` | `module` | 字段名不同 |
   | `category` | ❌ 后端无此字段 | 前端独有 |
   | `assignee` | ❌ 后端无此字段 | 前端独有 |
   | `creator` | ❌ 后端无此字段 | 前端独有 |
   | `sla` | ❌ 后端无此字段 | 前端独有 |
   | `attachments` | ❌ 后端无此字段 | 前端独有 |
   | `replies` | ❌ 后端无此字段 | 前端独有 |
   | `activities` | ❌ 后端无此字段 | 前端独有 |
   | ❌ 前端无此字段 | `stackTrace` | 后端独有 |
   | ❌ 前端无此字段 | `sourceTraceId` | 后端独有 |

3. **功能差异**
   - 前端支持工单详情页、回复、活动流、SLA 进度
   - 后端只有基础的 CRUD，无回复、活动流等功能

---

## 二、技术方案选择

### 方案A：前端适配后端（渐进式，推荐）

**思路**：保留前端 UI 和体验，逐步接入后端真实数据

**实施步骤**：
1. **Phase 1 - 基础对接**
   - 前端新增 API 调用层，封装后端接口
   - 前端 store 从 mock 数据切换为 API 数据
   - 字段映射：前端 DTO 转换为后端契约格式

2. **Phase 2 - 字段扩展**
   - 后端补充前端需要的字段（assignee、creator、category 等）
   - 数据库表增加字段
   - 后端 DTO 返回完整信息

3. **Phase 3 - 高级功能**
   - 后端实现回复、活动流接口
   - 后端实现 SLA 计算逻辑
   - 后端实现附件上传

**优势**：
- ✅ 渐进式，风险可控
- ✅ 前端 UI 体验不受影响
- ✅ 可分阶段验收

**劣势**：
- ⚠️ 后端需要较多改动
- ⚠️ 数据库表需要扩展

---

### 方案B：后端扩展前端适配（一次性，激进）

**思路**：后端一次性补全所有功能，前端完全依赖后端

**实施步骤**：
1. 后端扩展数据库表，补充所有字段
2. 后端实现完整 CRUD + 回复 + 活动流 + SLA 接口
3. 前端删除所有 mock 数据和本地逻辑
4. 前端完全从后端获取数据

**优势**：
- ✅ 架构清晰，前后端职责明确
- ✅ 长期维护成本低

**劣势**：
- ❌ 开发周期长，一次性投入大
- ❌ 无法分阶段验收

---

### 方案C：混合模式（当前阶段，折中）

**思路**：核心功能对接后端，高级功能暂时前端 mock

**实施步骤**：
1. **后端接口对接（本周完成）**
   - 工单创建：前端调用 AI Agent 接口创建工单
   - 工单列表：前端调用后端分页接口获取列表
   - 工单详情：前端调用后端接口获取详情

2. **前端字段映射（本周完成）**
   - 前端 DTO 转换层，统一字段名和枚举值
   - 前端 fallback 兜底：后端字段缺失时使用默认值

3. **高级功能暂缓（下一迭代）**
   - 回复、活动流、SLA 暂时前端 mock
   - 附件上传暂时禁用

**优势**：
- ✅ 快速验收核心功能
- ✅ 平衡开发成本和用户体验
- ✅ 为后续迭代留出空间

**劣势**：
- ⚠️ 数据一致性需要额外注意

---

## 三、推荐方案与决策点

### 我的推荐：方案C（混合模式）

**理由**：
1. 您当前处于"完善修复项目问题阶段"，需要快速验证核心功能可用
2. 工单模块的核心价值是 **AI Agent 自动创建工单**，这部分必须对接后端
3. 回复、活动流、SLA 等属于"锦上添花"，可延后实现
4. 前端 UI 已经比较完善，不需要推倒重来

### 需要您决策的技术点

#### 决策点1：工单字段对齐策略

**选项A - 后端扩展字段（推荐）**
- 后端补充 `assignee`、`creator`、`category`、`sla` 字段
- 数据库表增加字段
- 前后端字段完全对齐

**选项B - 前端适配后端**
- 前端只使用后端现有字段
- `assignee`、`creator` 等字段前端硬编码或隐藏
- 前端 UI 调整为简化版

**选项C - DTO 转换层**
- 后端保持最小化字段
- 前端封装转换层，缺失字段使用默认值
- 前后端各自独立演进

---

#### 决策点2：工单创建入口

**选项A - AI Agent 对话中创建（推荐）**
- 用户在 AI 对话界面描述问题
- AI 判断需要创建工单，调用 `createDevOpsTicket` 工具
- 工单信息自动填充，用户无需手动填表
- 前端工单列表同步展示

**选项B - 手动表单创建**
- 前端保留"新建工单"按钮
- 弹出表单让用户手动填写
- 提交后调用后端 API 创建

**选项C - 双入口**
- 同时支持 AI 创建和手动创建
- AI 创建的工单标记为"AI生成"

---

#### 决策点3：工单状态管理

**选项A - 前端只读，后端控制（推荐）**
- 前端只展示工单列表和详情
- 状态变更、分配人员由后端或运维平台控制
- 前端实时轮询或 WebSocket 同步状态

**选项B - 前端可操作**
- 前端支持修改状态、分配人员
- 前端调用后端 API 更新
- 需要后端补充更新接口

---

#### 决策点4：工单 ID 格式

**选项A - 统一为后端格式（推荐）**
- 前端展示 `TKT-20260717-0001`
- 前端 mock 数据全部改为后端格式
- 前端路由 `/tickets/:id` 使用后端 ID

**选项B - 前端转换**
- 后端保持 `TKT-` 格式
- 前端内部转换为 `OPS-` 格式展示
- 前后端 ID 映射表

---

## 四、待您决策后的实施计划

一旦您做出决策，我将按以下顺序执行：

### Week 1 - 核心对接（3-5天）
1. 更新 CLAUDE.md，切换到完善优化阶段
2. 前端实现后端 API 调用层
3. 前端 store 切换为 API 数据源
4. 前后端联调测试：创建工单、查询列表

### Week 2 - 高级特性（5-7天）
5. 实现多轮对话与会话上下文管理
6. 实现缓存优化策略（精细化失效）
7. 实现分布式锁（Redis Redlock）
8. 性能测试与优化

### Week 3 - 补充功能（按需）
9. 后端实现工单回复接口
10. 后端实现活动流接口
11. 后端实现 SLA 计算
12. 前端对接以上接口

---

## 五、需要您明确的问题

请您对以下问题做出选择（可直接回复编号）：

1. **总体方案**：A（前端适配）/ B（后端扩展）/ C（混合模式） - 我推荐 **C**

2. **字段对齐**：A（后端扩展）/ B（前端适配）/ C（DTO转换） - 我推荐 **A**

3. **创建入口**：A（AI对话）/ B（手动表单）/ C（双入口） - 我推荐 **C**

4. **状态管理**：A（前端只读）/ B（前端可操作） - 我推荐 **A**

5. **ID格式**：A（统一后端）/ B（前端转换） - 我推荐 **A**

---

## 六、CLAUDE.md 更新草案

我已准备了 CLAUDE.md 的更新内容，将：
- 移除"MVP 边界"限制
- 新增"完善优化阶段"指导原则
- 补充多轮对话、缓存优化、分布式锁等技术要求
- 更新技术决策记录

待您确认上述方案后，我将立即更新 CLAUDE.md 并开始实施。
