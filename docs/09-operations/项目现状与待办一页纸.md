# 项目现状与待办一页纸（2026-09-09)

> 写给只看 5 分钟的你。所有行话都避开；看不懂的地方就是我没写好，随时问。

---

## 一、现在做成什么样了

一句话：**程序本来该写的都写完了，而且每改一次都通过了自动测试**。

具体几点：

1. 后端、前端、数据库、自动测试都完整在位：
   - 后端 Java 文件 256 个，测试文件 153 个；前端文件 250 个；
   - 数据库有 11 个版本化的建表脚本，升级可追溯；
   - 对外接口 138 个；配套的自动检查（持续集成）最近连续 16 次全部通过。
2. 核心功能链都通了：知识库问答（带出处）、告警去重建单、诊断报告、
   「AI 动手之前必须人批准」的审批链、自愈动作的执行回放。
3. 面向验收的清单共 26 项（来自项目路线图）:
   - 18 项：东西已经做好，只等你最终确认签字；
   - 7 项：东西也做好了，但需要你**在自己电脑上真跑一遍**留证据（下面第二节）;
   - 2 项：需要你**拍板决定**（下面第三节）。

## 二、需要你做的事（只有三件，按顺序）

### 1. 在你电脑上跑一轮（约 2 个半天）

完整步骤在《真窗联合测试清单.md》（同目录），共 14 个小项，编号 T2~T15。
每项都写了：要敲什么命令、看到什么算通过、把什么结果发给我。
你不需要写任何代码，脚本我都备好放在 `scripts/lab/` 目录里。

前提是：你电脑上装了 Docker、JDK 21、Maven、Node（跑前端时用）。

### 2. 合并代码到主分支（点一下键）

GitHub 上的 PR #2 一直是绿的，合并后有三件事会自动生效：
安全漏洞扫描、周期巡检、评测基线对比。我建议随时可以合并。

### 3. 两个拍板（各回我一句话就行）

| 问题 | 我的建议 | 你回什么 |
| --- | --- | --- |
| 要不要现在引入消息队列（MQ)? | 不要，现在线程池够用，等阶段 6 再说 | 「同意」或「现在就上」 |
| 产品按多租户 SaaS 做还是私有化部署做？ | 私有化部署（和 PRD 现有约定一致） | 「同意」或说明你的方向 |

## 三、什么时候算全部搞完

按上面的节奏：

- 你每天能投入较多时间：大约 **9 月 13～14 日**全部收尾；
- 你只能断断续续抽时间：大约 **9 月 16～17 日**。

之后剩下的都是长期演进的小事（比如再接几个真实动作的执行器），
不影响「项目完成」这个判断。

## 四、我在此期间做什么

你跑测试时把结果贴给我，我负责：核对是否通过、把通过的格子在验收清单里勾掉、
出问题就定原因并修好。你的每一步产出我都不浪费。
