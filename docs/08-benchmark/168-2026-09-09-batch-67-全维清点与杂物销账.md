# 批次 67：全维清点——前后端/文档/远程分支杂物三件销账（含一次 repo 现场自救实录)

日期：2026-09-09 ｜ 状态：完成（双腿绿） ｜ 承接：批 66 后用户三件（前后端动态账/深检杂件/全分支盘点）

## 一、动态账（实测，remote compare API)

- origin/main vs 当前分支:**569 commits ahead**,仓史 4.19 MiB 体量包件——无构件膨胀;
- 区质：后端 256 件源+测试 153 件、前端 250 件、迁移 V1..V11——实测口径与批 62 一尺；
- CI 双腿绿累计 18 批（含批 64 新迁移校验步）。

## 二、动刀三件（全带证据）

1. **`sql/init.sql` 全删**:S0-1 收尾挂账清偿。14 行过渡桩前提「CI 旧步骤还执行
   psql init.sql」已被批 64 CI 现状取代（现 CI 起 pgvector service+重建账+validate);
   真相源唯一=classpath Flyway V1 基线，无第二真相源。compose.dev 门面行同步改口;
2. **`FutureCapability.vue` 删**:L4 四占位报告 113 已全解封,git grep 全库 import
   面零引用，此组件无任何入口——占位组件的历史使命已由批 113 收讫;
3. **远程分支 `arena/01a031f6-opsbrain-ai` 删**:git merge-base --is-ancestor 实查
   完全包含于当前分支，删=内容零丢失;`git ls-remote` 复核：远程主力 = main + 当前分支。

## 三、「不删」有案四格

- `sql/mock_data.sql`：三处引用活（compose 注释/docker-initdb.sh/手册）；
- `tools/audit/` 七件:CI 三件（bundle_budget/eval_compare/eval_baseline）+手工审计道
  （人工审计道：批 36 审遗手元器件，基线语义=只报新引入，不放水也不养习惯红）;
- `docs/03-..04-OpsBrain-*.md`：历史方案档 §十一注记留档；
- `SOP_PreFlight_Check.sh`:AGENTS.md 调用道活。

## 四、repo 现场自救实录（本批警示一条）

清刀一半,git fetch 意外现「clone 直指今时现场」——本地 .git 被环境侧某链条重新 clone 到 674429c(diff 根基被掀——先前批次 53-66 只存在于远程)。现场证控:
`git status --short | head -3` 只看「头 3 行」的排查手法就是这次事故的放大器——半截净床不等于全净。
救场五条：先停手→reflog 探明现场→fetch 拿 FETCH_HEAD 复位 87c569c→复验三件盘→批 67 三刀复切。**「git status 截头查净不净」一律全列**——入铁律。

## 五、红账追加（事后实录，自查落后）

**第一次落刀顺序错了**：先删 `sql/init.sql` 才想起 CI 两个 workflow（`ci.yml`「初始化数据库 Schema」步 + `eval-baseline-update.yml` 同款步）仍在 `psql -f sql/init.sql`——而 `sql/migration_v*.sql` 早已不在库内，那段迁移循环是空壳，桩文才是唯一真跑的那一脚。桩文「待 CI 删旧 psql 步后删除」我读了却当成"前提已过"，把它当现状描述而不是挂账前提，一推即双腿红。

**修红三推**（均 watch 实证）：
1. `7caea5a`：两 workflow 的 schema 步由桩改指 `src/main/resources/db/migration/V1__baseline.sql`；占位页临时复位做变因隔离；前端 knip 步加同款 tee 取证道（im失败自动回 commit 帖）。→ 双腿绿 ✓
2. `c7f4965`：重删 FutureCapability.vue → 前端 knip 仍红，但取证帖当场回出真凶：`Unused files (1) src/components/common/ComingSoonPanel.vue`——占位页是它的唯一消费者，删页留座=孤儿组件。
3. 补刀同删 ComingSoonPanel.vue → **双腿绿 ✓**

**加挂铁律（批 68 入账）**：
- 删物前引用面实查 **必须全层**（含 `.github/workflows/**`、测试档、脚本），grep 不打 exclude 招子；
- 「挂账语句写在待删物内」本身是前提**未灭**的声明，读到=它还在位，不可反签；
- 变因多时也按"一次一动"隔离：复位占位页的单推证明了 knip 红与 workflow 修红无关，证据链干净。

备注：knip 另有 7 条 Configuration hints（非失败项），属配置瘦身提示，列入批 68 +可选清扫。
