# 批次 67：全维清点——前后端/文档/远程分支杂物三件销账（含一次 repo 现场自救实录)

日期：2026-09-09 ｜ 状态：完成（双腿绿） ｜ 承接：修期报表+深检+远程分支盘点

## 一、动态账（实测，remote compare API)

- origin/main vs 当前分支:**569 commits ahead**,仓史 4.19 MiB 体量包件——无构件膨胀;
- 区质：前端 250 件 (S1-~S4 各批新面),后端 256 件+测试 153 件，迁移 V1..V11;
- CI 双腿绿累计 18 批（含批 64 新迁移校验步）。

## 二、动刀三件（全带证据）

1. **`sql/init.sql` 全删**:S0-1 收尾挂账清偿。14 行过渡桩前提「CI 旧步骤还执行
   psql init.sql」已被批 64 CI 现状取代（现 CI 起 pgvector service+重建账+validate);
   真相源唯一=classpath Flyway V1 基线，无第二真相源。compose.dev 门面行同步改口;
2. **`FutureCapability.vue` 删**:L4 四占位报告 113 已全解封,git grep 全库 import
   面零引用，此组件无任何入口——第一眼的「起重机」一变就是挡住将来清账的铆头;
3. **远程分支 `arena/01a031f6-opsbrain-ai` 删**:git merge-base --is-ancestor 实查
   完全包含于当前分支，删=内容零丢失;`git ls-remote` 复核：远程主力 = main + 当前分支。

## 三、「不删」有案四格

- `sql/mock_data.sql`：三处引用活（compose 注释/docker-initdb.sh/手册）；
- `tools/audit/` 七件:CI 三件（bundle_budget/eval_compare/eval_baseline）+手工审计道
  （甄别过道微道两面，即 [A.批36 审]，均有防防伪纪律的基线语义）;
- `docs/03-..04-OpsBrain-*.md`：历史方案档 §十一注记留档；
- `SOP_PreFlight_Check.sh`:AGENTS.md 调用道活。

## 四、repo 现场自救实录（本批警示一条）

清刀一半,git fetch 意外现「clone 直指今时现场」——本地 .git 当场重建到 674429c 雅居边
（Arena 重幽链条一手），先前批次 53-66 在远程 LAN 二百淳。现场证控:
`git status --short | head -3` 噎咙「头 3 行」的卦排除巧用就是这次事故放大器。
救场随这五步:篝火先停手→reflog 探明现场→fetch 拿 FETCH_HEAD 复位 87c569c→
复验三件台→译批 67 三刀复切。**「git status 截头查净不净」一律全列**——入铁律。
