# 批次 35：S4-2 线上校准读数——反馈闭环第二读数，真窗前的数据面就绪

日期：2026-09-08 ｜ 状态：完成 ｜ 承接：批 33(ECE 骨架）、批 34（分桶校准器）、报告 130（反馈闭环第一读数）

---

## 一、定位一句话

S4-2 只剩两条数据源通道：真窗（堵用户）与**线上反馈闭环**（不堵任何人）。
本批把后者接进看板：诊断假设的 `confidence`（引擎钳制 0~1）× `feedback`
（HELPFUL/PARTIAL/WRONG) 聚成校准读数——ECE 从「评测骨架」走到「生产可见」。

## 二、三缝落件

| 层 | 件 | 语义要点 |
| --- | --- | --- |
| 后端纯计算 | `HypothesisCalibrationBoard` + 6 钉测 | 判定集=HELPFUL/WRONG;**PARTIAL/未知反馈/越界置信度均豁免但计数**（不静默消失、不吞半分、不崩页——生产面口径与评测面硬拒互为表里）；空判定集 → ece/acc/meanConf **null**（「还没人反馈」≠「误差 0」);ECE 公式与 test 侧 EceMetrics 同谱，类注立同步戒律 |
| 接线 | `DashboardServiceImpl.getDiagnosisBoard` | 窗口内反馈假设直查 → `board.put("calibration", …)`；不新开端口，同端点同窗口 |
| 前端 | `Dashboard.vue` 校准块（3 KPI)+ 类型 + smoke +3(1811) | ECE / 经验正确率 / 有用·已判定；null→「—」、旧后端无键→整块不渲染（不发空壳）;PARTIAL 豁免口径随 tooltip 透明 |

## 三、与真窗线的边界（别混账）

- 本读数 = **生产反馈闭环**的校准证据，累积随线上反馈量；
- 真窗（EVAL_LLM）读数 = **评测口径**的校准证据（4-2.4 前后对比，手册报告 136 §四）;
- 两者互补不互替：看板卡位预留给真窗的幻觉率/ECE 仍未发空壳，红线在案。
- 温度计打卡：4-2.5 目标 ECE ≤ 0.15，两个口径谁的读数先到，先看谁。

## 四、闸序账

- 六闸全绿（tsc 0 / lint 0 / **knip 0**——新议题 1 条已按批 29 第四豁免形态标准件消化（CalibrationBucket 载荷嵌套推断）/ vitest **1811**(+3) / build ✓ / 体积预算 ✓)
- 后端纯计算 + 服务接线：零 JDK 环境，仲裁交本批 CI 后端腿。
- 环境插曲：node_modules 被前期重克隆事件清空（snapshots 排除项）,`npm ci` 复原——批 33 事故影响面的尾单已清。

## 五、次批线索

S4 面沙箱侧只剩\"等数\"：真窗数据到 → 4-2.4 接线；反馈量爬升 → 校准块读数自行生效。
路线走 S5-3.3（依赖漏洞扫描 CI）或 S0-1/S0-4 收尾（验收件）听令。
