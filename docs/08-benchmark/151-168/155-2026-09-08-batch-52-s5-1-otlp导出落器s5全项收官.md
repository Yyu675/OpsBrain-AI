# 批次 52：S5-1 尾件 OTLP 指标导出落器——S5 全项收官（演练外无残）

日期：2026-09-08 ｜ 状态：完成 ｜ 承接：报告 154「S5 收敛为一件」

## 一、口袋从 JDK 批开出来的理由

BOM 仲裁捶：spring-boot-dependencies 托管 Micrometer 全家桶版本,
`micrometer-registry-otlp` 入 pom 无版本猜题目;pom/yml 两份静态证件
(ElementTree 解+pyyaml 证）全过,CI 仲裁席续任(JDK 批定一件之敌的理由销账)。

## 二、落件双证

- pom:`micrometer-registry-otlp`(BOM 托管版本);与 prometheus 注册器同族同号,双注册器共存;
- application.yml:`management.otlp.metrics.export` **默认关**(enabled=false,显式 env 开)——
  没接 collector 就推流 = 周期连接错误刷日志 + 「指标有去处」的幻觉;step=60s(画像/趋势档,告警面照归 prom 拉模型)。

## 三、S5 全段终账(十子项→零残,仅演练挂格)

| 组 | 终态 |
| --- | --- |
| S5-1 可观测 | 探针分离(实证)+ECS 结构化日志(48)+水位计(49)+业务告警组(50)+**OTLP(52)=全清** |
| S5-2 部署运维 | 六子项(37)=全清(演练一格挂) |
| S5-3 安全 | 3.1/3.2(续收口 46)/3.3(36)/3.4/3.5(45)=全清 |
| S5-4 性能 | 4.1/4.2(42+V11)/4.3/4.4(43)=全清(EXPLAIN 挂格) |
| S5-6 移交 | 清单+签署页(44)=全清(两挂格在案) |

**S5 从此零残**——残余面只剩真窗演练挂格与用户阶件(并 main/演示/D-07、多租户拍板)。
