# 批次 48+49：S5-1 下半场两落器——ECS 结构化日志 + 业务水位计

日期：2026-09-08 ｜ 状态：完成 ｜ 承接：用户「继续按照建议执行」选育出的两条改道

## 一、从「等 JDK 批」里划出的两件(建议线的改道案)

原账:S5-1 四件全归 JDK 批(logstash 依赖需入 pom,盲改主启动面不押 CI)。
新账:**Boot 3.5.6 自带结构化日志(3.4+)与 Micrometer 无需新依赖**——精准扫描后,
两件落器,JDK 批收窄为只剩 OTLP 指标导出一件。

## 二、批 48:ECS 结构化日志(纯配置,零编译险)

- application-prod.yml:console/file 双 `ecs`(Elastic Common Schema);
- MDC/traceId 随身入 JSON——告警风暴现场 jq 一拉全链;dev profile 人类可读原样;
- 硬开不给关闭口子(auth-enabled 同款纪律:回滚走改档 git 审计);
- 收集管道尚未接(真窗件),格式先行免费:管道到那天,旧日志已能看懂。

## 三、批 49:业务水位计(侧车模式,零构造器手术)

勘察实锤:6+ 测试类手装 `new TicketService(...)`——直插构造器=大面积彩修,
改道:**BusinessMetrics 独立组件四枚存量 Gauge**:
tickets.total / tickets.urgent_pending / alerts.active / approvals.pending。
- 供体=既有 repo 计数法(countAll/countUrgentPending/countActive/countByStatus)零增码;
- 与看板 KPI 同口径,库表可一对账,造假空间先验为零;
- 供体异常→NaN 退化,不陪葬整抓面(Down 闸批37 在岗,本件不藏业务归零);
- 增量计数器(建单速率)与 OTLP 导出归 JDK/真窗合并批续账。
- 单测 2 例:SimpleMeterRegistry 读数对账 + 供体崩→NaN。

## 四、S5-1 终账

探针分离(实证预存 批37)+ 结构化日志(48)+ 关键指标(49)= 三件清;
**S5-1 唯一残留=OTLP 指标导出(micrometer-tracing+OTLP dep,JDK 合并批)**。
