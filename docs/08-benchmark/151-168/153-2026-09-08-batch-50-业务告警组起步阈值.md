# 批次 50：业务告警组落器——水位计接通告警契约，起始阈值两枚

日期：2026-09-08 ｜ 状态：完成 ｜ 承接：批 49 指标入库 → 批 50 指标生警

## 一、落件

`monitoring/alert.rules.yml` 新组 `opsbrain-business-alerts` 两枚:
- `OpsBrainUrgentPendingHigh`（高优工单积压 >10 / 15m → P2):值班拥堵信号;
- `OpsBrainApprovalBacklog`（待审批积压 >5 / 1h → P2):治理闸空置替身警报。
labels 全守既有契约（severity warning→P2 / module=APP / annotations 落工单）。
数据面 = 批 49 BusinessMetrics 四枚存量 Gauge(点转下划线名自动对账)。

## 二、两条纪律（写在组头注释，永远生效）

1. **起步阈值真窗首周校准**：按 P95 水位回填，拍脑袋阈值=风暴种子；启动值宁缺勿宁扰。
2. **NaN 不误报**：供体崩坏→Gauge=NaN→比较恒 false；依赖/Down 闸另行报警，职责分离双保险不重叠。

## 三、撰档狗食一次

初版 description 两处叠字/断文（「同口径口径」「整理那加正名」）即时返工——
文字进告警卡片的下一跳是值班工单，语句不过关从此带着蠢字进事故档案。

## 四、S5-1 终态（全组闭环）

| 子件 | 出路 |
| --- | --- |
| 探针分离 | 实证预存（批 37 勘察） |
| 结构化日志 | Batch 48 ECS(Boot 3.5.6 原生） |
| 关键业务指标 | Batch 49+50（四个 Gauge + 两条起步告警） |
| OTLP 指标导出 | **JDK 合并批唯一残留**（依赖入 pom 需编译实证） |
