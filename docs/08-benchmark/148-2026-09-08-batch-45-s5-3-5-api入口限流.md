# 批次 45：S5-3.5 API 入口限流——双闸出厂（登录 10/60s · AI 对话 20/60s)

日期：2026-09-08 ｜ 状态：完成(单测 6 例待 CI） ｜ 承接：报告 139 §二 S5-3.5

## 一、可行性翻案（原列 JDK 批，本批落地）

**resilience4j-spring-boot3 2.4.0 + annotations 早已在 pom**（批 33 检查出真相：
并发闸于 S0-4 llm 限流早已上岗）——S5-3.5 无需新依赖，划出 JDK 批，批内交付。

## 二、落件三件

| 件 | 设计要点 |
| --- | --- |
| `RateLimitProperties` | 规则大队（name/paths/permits/period-seconds),enabled 独占压测现场开关 |
| `RateLimitFilter` | （规则，IP）分桶发 resilience4j RateLimiter(timeout=ZERO，超员即 429 不排队）、Retry-After+JSON 拒收单、context-path 剥离、桶表 2 万硬顶摊还重建 |
| `RateLimitWebConfig` | FilterRegistrationBean 登记，Order=TraceId+1;**不标 @Component**(web 切片扫 Filter 不扫 ClientIpResolver→25 类喂马红案避让，冲击半径归零） |

配位真相接入：限流键 = `ClientIpResolver`(XFF 辨伪归其管，审计/限流同一口径，
不写两本花名册）。

## 三、一阵三道防波堤（集成面配额安全账）

| 面 | 计算 | 结论 |
| --- | --- | --- |
| web 切片（25 类） | 滤件不经切片 | 不受波及 |
| AuthLoginChain 4 登录 + RoleGuard 1 | 不同配置严定，不共享上下文 | ≤5 上限内 |
| Chat Sse 两堂 + chat 调用 <20 | 限 20/60s | 上空裕大 |
| 上下文共享时 IP 桶累积（最悲观 7 登录共享一腔） | 待 CI 实测价 | **红灯即调闸的预留位**:properties 可调 |

## 四、单测 6 例（SPRING mock 直测零容器）

配额放行→超限 429→IP/规则分桶独立→context-path 剥离→非/非 api 免税→disabled 全放→1s 窗回补实测（限流=配速不是封号）。

## 五、已知边界（焊车身上，四条全写类注释）

IP 桶非用户桶（Sa-Token 钉序后续增强）/桶表硬顶粗回收/多副本全局配额≈配额×副本数
(redis 桶=多副本时代清单）/XFF 由 Resolver 统一彩。都是明示，不是隐病。

## 六、S5 两市到收尾拐点

S5-1×4/logstash+S5-3.2续/4.5trgm+5.1重看五件仍归 JDK/真窗；限流清后，
**纯沙箱可推进面最后两支=S5-1 日志事故演练档/S5-3.2 脱敏 Python 对账（下回合先勘)。
