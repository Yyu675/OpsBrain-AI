# 批次 42：S5-4.2 慢查询静态审计——84 枚存量对账过、确证 2 枚 V11 落器

日期：2026-09-08 ｜ 状态：完成（静态半程；EXPLAIN 演练挂账） ｜ 承接：报告 139 §二 S5-4.2

## 〇、施工前置插曲（环境事件，防失忆）

回合开工发现沙盘 `.git` 对象时空心：本地 log 只剩基线 674429c，工作区文件全在场、
远端 1e9dfe2 全在场。**`git fetch` +FITCH_HEAD `reset --mixed` 复原**（零文件损失，
status 回零）——沙盘工作区持久、.git 对象库可回收的证据钉。教训记入批档案。

## 一、审计方法（EXPLAIN 缺位下的静态半程）

全量解析 V1..V10 索引面 x 全部 JdbcTemplate/JPQL 热点查询的过滤列集（27 基线表、
84+ 存量索引）。SLA/dedup/审批/AI 反馈/知识治理五个领域的哨兵+部分索引核验全在场。

## 二、确证缺口 2 枚 → V11

1. `idx_alert_group_dedup`（部分索引）：风暴聚合退路查询（findActiveGroupTicket),
   dedup-miss 时每条告警必跑，窗口列 last_occurred_at 与既有 service 索引不对口。
2. `idx_ticket_create_time (DESC)`：看板 KPI 今日新增/趋势，首页每次加载必扫，
   create_time 素无对口索引。

观察档 3 项不动（keyword 术语→pg_trgm 真窗决议项 5-4.5;urgent bitmap 已服务；
诊断看板低频）——每一项都是「砍谁建谁」的闸序在案。锁窗纪律：Flyway 内禁
CONCURRENTLY,V11 后迁移一律 CONCURRENTLY 双段。

## 三、连带断论修正（档案级）

「V24/V42 SLA 索引已建」在真实仓里的形式=V1 squash(674429c revert 案并入）;
内容在场、编号不在——对账以内容为准。另实勘：表载体双轨（KnowledgeChunk 独有 JPA,
余 26 表 MyBatis/JdbcTemplate)，索引责任规条落 migration 文件。

## 四、演练挂账（未跑不勾销）

V11 前后 EXPLAIN 对照（双查询行数/耗时）、膨胀率月观测入值班周会、pg_trgm 拍板——
全部待真窗，未跑不勾销；本批静态半程≠S5-4.2 结题。
