# 批次 43：S5-4.4 连接池配置核查——五池盘点，Hikari 默认裸奔封堵

日期：2026-09-08 ｜ 状态：完成（演练三项挂账） ｜ 承接：报告 139 §二 S5-4.4

## 一、盘点（真相勘定）

- Hikari(prod)= 全默认裸奔(pool10/30s);dev 方言 10/2/30s。风险:生产拿默认值过年。
- 并发闸真相=resilience4j ratelimiter llm(60/s+10s 配速,S0-4 实测校正)——bulkhead
  无配置存在,web 争用已被 ratelimiter+webhook 滑窗闸压底,池压力 ≤ 闸放行量。
- PG max_connections=100 未覆写;commons-pool2 在仓但 Lettuce 池化未开(共享连接,评级过)。

## 二、落器一件:prod Hikari 显式块(全 env 背撑)

pool 20/idle 5/conn-timeout 20s/max-lifetime 600s(LB 空闲断先)/idle 300s + keepalive 240s(铁序 keepalive<idle)。
规模公式立铁律:replicas × pool ≤ max_connections × 0.7——扩副本先核公式,公式不破池不炸。
leak-detection 不进 prod(调查期武器,用完放回——dev 现场原样)。

## 三、撰档狗食一次

「蜘蛛侠闸」幻觉串与指标句乱码两残留→grep 清扫归零+pyyaml 双过(撰档即审计的
又一次红利:写完必搜,搜完必净)。

## 四、S5 推进表(更新——S5-4 静态面全清)

| 组 | 子项 | 状态 |
| --- | --- | --- |
| S5-4 性能 | 4.1存管√(批20) / 4.2静态审计√(批42,EXPLAIN挂) / 4.3 bundle√ / 4.4池核查√ | **静态面全清,真窗差三项演练** |
| S5-3 安全 | 3.1密钥√ / 3.2脱敏√(97主业,复核续 Java 批) / 3.3依赖√(36) / 3.4补丁√ / 3.5限流(JDK 批) | 剩 2 Java 批 |
| S5-1 可观测 | 探针分离已证 / OTLP+结构化日志+关键指标+超时 = JDK 批四件 | 等 JDK |
| S5-2 部署 | 六子项全落(批37) | ✅ 演练格挂 |

S5 未余项呈明显二分:**五件 JDK 批**(S5-1×4/S5-3.5)×**真窗演练档**——
无环境可推进面趋近清零,下一批将进入「文档类余量」与 JDK 就绪件清单的合并推账。
