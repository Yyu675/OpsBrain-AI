# 批次 37：S5-2 部署与运维——预算立、监控自证、备份有反面、SOP 有现场

日期：2026-09-08 ｜ 状态：完成（演练记录格待环境） ｜ 承接：报告 139 §二 S5 残账

---

## 一、勘察清障（三颗本以为要做、其实已具备的件）

| 以为缺口 | 实测真相 |
| --- | --- |
| app 健康检查 | Dockerfile 已有 HEALTHCHECK(/ping 只证进程活，云身分正确） |
| 5-1.5 探针分离 | `probes.enabled: true` 已在 application.yml(liveness/readiness 自动成组） |
| 5-2.5 自身监控 | alert rules/prom 已在仓但**从未抓应用自身**——真缺口只剩 scrape |

## 二、落件六件

| 件 | 内容 |
| --- | --- |
| `docker-compose.yml` | `x-default-logging` 锚点(json-file 50m×5，全容器统一有界）+ app `deploy.resources`(2g/1.5 上限、768m 预留；JVM MaxRAMPercentage=75 口径互证） |
| `monitoring/prometheus.yml` | `opsbrain` job(`/ai/actuator/prometheus`,context-path /ai 不照官方文档） |
| `monitoring/alert.rules.yml` | `opsbrain-app-alerts` 组：OpsBrainDown(P0/2m)+ FrequentRestart(P2);labels 契约与 AlertService 同构（未知 module 落「其他」容错已在案） |
| `scripts/backup.sh` | PG(-Fc 热备）+ MinIO（整桶 tar.gz)+ **manifest 三件互锁**(SHA256+git_sha）+ 14 天保留修剪；无 manifest=半截件，restore 第一步即拒 |
| `scripts/restore.sh` | --from/--confirm 双必选；SHA 对账 → stop app → 还库 → 还桶 → 起栈健康等 → **恢复三查**（工单/文档/最近告警）人工对账 |
| `docs/09-operations/` | 部署运维手册（起栈/预算/密钥 L0→L2 选型/备份演练模板）+ 值班 SOP(AI 不可用/向量库/告警风暴/磁盘满四现场，每条先说「已有机制先信」) |

## 三、两条纪律沉淀

1. **预算与告警互为表里**：有日志/内存预算，才谈得上「超预算告警」；调预算先看基线，别拍脑袋（入手册 §2)。
2. **备份存在 ≠ 能恢复**:manifest 锁件 + 演练记录模板已立——「有演练记录」格待 Docker 环境首演勾稽，与 S0-2b/压测同属环境型，不误报已验收。

## 四、闸序账

YAML 三证（compose/prometheus/rules)✓;`bash -n` 双脚本 ✓;env 干涉为零（纯配置/脚本/文档）。CI 仲裁：本批 headSha 过滤报账（后端腿应无差——Java 零碰）。

## 五、S5 推进表（更新）

| 步 | 状态 |
| --- | --- |
| 5-2 部署运维 | ✅ **六子项全落**（演练格待环境，册中红字在案） |
| 5-1 可观测 | 🟡 半（5-1.5 探针实证已具备；OTLP/结构化日志/关键指标 = S5-1 批二选） |
| 5-3.3 依赖扫描 | ✅（批 36) |
| 5-4.3 bundle | ✅(F-2/F-4) |
