# 129 knip 门禁转正：从借条到钢铁

> 报告性质：前端模块技术债收官（阶段 D），批次 26（T42），knip 战线终章。
> 前四波（报告 122/125/127/128）把三类真账清零，本批收尾三件事：
> **假阳全部消化、Unlisted 真账归位、CI 门禁正式转正**。

---

## 一、本批三刀

| # | 件 | 落法 |
|---|---|---|
| 1 | **21 条假阳全消化** | 不删代码、不报假账——逐条 `@public` 标记并随证据注释（cross 引用数 + knip 5.88.1 re-export/barrel 解析盲区定性 + 「版本收敛后删行复查」退出约定），豁免动作与批 19 标准件同一武器 |
| 2 | **Unlisted deps 17 真账归位** | `package.json` 此前声明 **0** 个 @codemirror 包，vendor 文件却静态 import 17 个——靠传递依赖「侥幸运行」（上游 lock 一动即炸）。按 `node_modules` 现存版本精确补声明（lockfile 零震荡），真账归位 |
| 3 | **CI 门禁转正** | `continue-on-error: true` 删除，注释改写为「退出条件已达成」——**新增死代码/未声明依赖即红**，knip 从借条升级为钢铁 |

## 二、战线总账（knip 领域自此闭环）

| 类 | 开工（报告 122） | 收官 |
|---|---|---|
| 重复导出 | 1 | **0** |
| 未用导出 | 21 | **0**（真账 14 消化于判例三斧，假阳 7 @public 消化） |
| 未用类型 | 92 | **0**（真账 71 消化于四簇去化+成对死链双删，假阳 14 @public 消化） |
| Unlisted deps | 17 | **0**（17 包正式声明） |
| **knip 本地 RC** | 1 | **0** |

判例资产（永久留在决策轴）：
① HTML SSoT/公共库/前后端成对 → @public 豁免；② 同楼两式残影必删不豁免；
③ 成对死链必须双删；④ 假阳不硬清、存证+路径 B 校准；⑤ Unlisted 先查
「声明缺失 vs 解析盲区」——本批的 17 正是前者。

## 三、验证链

`npx knip` 本地：Unused×3 类 = 0、Unlisted = 0、**RC=0**；
`vue-tsc` 0、lint RC=0、vitest 93 文件 1806 绿、build ✓；
ci.yml YAML 解析过——门禁首跑由本批 CI 双腿实证（frontend 腿跑 knip
正式门禁，红不红立竿见影）。

## 四、后续维护约定（写进 ci 注释与本报告，供后来者）

- 新增死代码 → CI 红，不用商量。
- 遇到 knip 新假阳 → 同式 `@public` + 证据注释，禁止为消账删活代码。
- knip 版本升级后若假阳收敛 → 删对应 @public 注释行复查（注释里写有这件事）。
- vendor 新增 codemirror 包 → 必须同步 `package.json` 声明（门禁也会抓）。
