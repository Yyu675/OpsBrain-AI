# 批次 46：S5-3.2 脱敏复核续——日志灾面全扫 + 一处凭据直录收口

日期：2026-09-08 ｜ 状态：完成 ｜ 承接：报告 97 主体已过(3 猴领)/S5-3.2 续

## 一、三面扫描(值直入口径,只查值不进名)

| 面 | 结果 |
| --- | --- |
| log.* 敏感词全扫(密码/secret/apikey/jwt) | 三处全合格:webhook 提示只说「未配置」、种子口令只说「取自 env」、DingTalk basename=是/否 二元——全部只点名不带值 |
| 异常消息带配置灾面 (JDBC URL/credentials 入 message) | 零发现;异常构造全不携配置值 |
| 模型审计内容灾面 | OperationAuditRecord.requestDigest 恒 null(设计:审计单绝不存请求体)——审计面天然泄不了;prod langchain4j=WARN 在案 |

## 二、一处实获收口(首个真发现)

`HealingOrchestrator`「已撤销」日志原样直录 **undoToken 全长**(接口侧术语对照就是它自己的「撤销凭据」)——
对表内纪律「凭据类字面值不入日志」而言越序。修法:`maskToken()`(前 4 位+`len=N`):
定位事实格够(能对上具体那一次执行),暴露面收(前 4 位撑不起穷举)。
对轻量判语:用处狭窄的凭据少了「不明不白」四个字。

## 三、复核结论(S5-3.2 续闭环)

97 号批三猴顶主体过 + 本批三面零获 + 一处凭据直录收口=S5-3.2 全清。
残留读法:「脱敏」在这套工程里是**设计面**(审计不存体/告警不含凭据摘要流转/日志点名不带值)
而不是「事后过滤」——复扫永远只值「值直入审计」一种怀疑,本案是首个漏点也已收口。
