# 批次 64：路线图全维深挖——逐条对立 + 决策落账 + 两处真修(F1 迁移校验/F2 效果看板备件)

日期：2026-09-09 ｜ 状态：完成(F1 经三轮修红转绿)｜ 承接：用户三求（采纳建议/深挖究/缺了哪些一并修）

## 一、全维深挖逐条对立（§4~§11 整卷过)

判词分三格:✅完全就位 / ⚠️工程位已齐,真窗或用户动作未完即约 / ❌缺口(须修)。

### 阶段 0(§4)
- 0-1 Flyway:0-1.1~0-1.5 ✅;**0-1.6 ❌→本批 F1 修**(CI 迁移校验步,三轮修红转绿:
  ①插件 11.17.3 不存在(系 flyway-core 列车,插件列车 11.x 末版 11.20.3)——沙箱
  读 GitHub CDN 日志被墙,借 commit comment tee 取证道(批 64 常驻机制),
  ②加之 DB 模块暂不在插件 classpath(Flyway 10.2+ 拆分)、③12.14.0 覆盖黑客手段未入库记录。
  现在 CI 内:空库 migrate→validate→表数≥27 断言全绿——0-1 验收第一条的 CI 化同时实现);
- 0-2 TC:术语就绪(mvn test 待 T14 真窗);0-3 Resilience4j 全卫 ✅;
  0-4 评测 152 条+RAG 门/LLM 手动门/PR 对比评论/工件升级备 ✅(基线⏳T12/T13)。

### 阶段 1(§5)
- 1-1 指标：queryServiceMetrics+三态+异常检卫(15000ms 坚限贴贴骨模板)✅;
- 1-2 变更:sys_change_event+CI 回调+工具链 ✅;1-3 日志:2000 字截断+注入防+聚类 ✅;
- 1-4 拓扑：延后(D-04 记录向样);1-5 证据模型+聚合器/冲突标记/证据不足判词 ✅。

### 阶段 2(§6)
- 2-1 告警→自动建单+自动诊断+回填：聚合抑制一次触发、低抽异步 ✅(webhook 500ms
  为真窗 T6 数值格);2-2 假设置信度 1~3 条封顶+证据不足硬上 0.6(ConfidenceEngine 实查)✅;
- 2-3 报告+前端：四方向书架组织、路径直链 /diagnosis/:traceId ✅,德文指标升级 xmm。

### 阶段 3(§7)
- 3-1 执行器注册强契约「非只读必可撤销,否则启动即失败」ControllerInfrastructure 实查 ✅;
- 3-2 k8s 两真+Mock 双轨；爆炸半径场态阻口 ✅;3-3 审批双利+Saga 补偿+幂等拦截 ✅;
- 3-4 执行后调度判据半动以降两路(自动回滚/升人工;报告 109 介绍了这两板斧)✅;
- 3-5 前封 L4 占位清 ✅(报告 113，路由 33 条实查)。

### 阶段 4(§8)
- 4-1 判据：Recall@1/3、MRR、幻觉率、证据不足识别率全出,ECE 计算 ✅
  (评估件 RetrievalRankMetrics/EceMetrics 实查);expectedDocId 40 注记 ⏳T12;
- 4-2 分桶校准器 ✅(报告 137);4-3 CI 回归：PR 对比+基线门+**marker 原子平幂**贴评论 ✅,
  亮剑只欠数字(T12/T13);**4-4 ⚠️→本批 F2 案**：看板可视化备件
  (grafana dashboard JSON + 手册 §10 可选启用)+现取=Prometheus 29090 直接查趋势、
  PR 评测贴、应用 monitoring 页 —— 顶上满足「趋势可见+数据源失败率可见」判定。

### 阶段 5(§9)
- 5-1 全位(ECS/trace/水位/OTLP);5-2 十六档/备份脚本+服务实录 ⏳T3;
- 5-3 全卫(权限全线所/脱敏/依赖门/注入辩/双限流);5-4 四子件三件绿,
  压测与 EXPLAIN 真窗 T4/T5/T6 挂单(已配器+脚本,上手即跑)。

### 验收条格(§13)现状
- **工程面已齐 19/26**(13.1-1 本批翻转:runtime 边 + CI 境双线 validate/migrate 双保险);
- 真窗 7 项 + Trivy(并 main)1 项;**没有其他「未实现的修复任务」**。

## 二、决策落账(用户 2026-09-09 批准)

D-07 MQ→阶段 6 不提前 / 私有化定位 / D-01..06 认可 ——预置件 §四落案。

## 三、留下的可行建议(真缺位强化,不属路线图硬账)

1. **F2 扩展化**：拥 Grafana service 为默认阵(手册 §10 已备 8 行 copy 即用);
2. **F3 排程灯**：效览开拓——eval_metrics 历史趋势一页（数据集一过 T13 有数后立);
3. **写入 README 评测数字句**(0-4.6 原文):必须 T13 真数字到手再落笔——防盲注纪律;
4. **S6 接挂**：真执行器三件套（清单批 59 实体档）随哪只手先上择一。

## 四、本批红史（即学即装）

- 三轮修红链：插件版本列车混查(11.17.3→11.20.3)→DB 模块暂不在插件
  classpath→**取证机制自身上**(沙箱 CDN 墙,commit comment tee 为取数道);
  往后任何只能在 GitHub 证的分歧面，先上取证道再动笔改——不猜多试。
