# 60 · 「AI 运维工单处理智能体」闭环链路核查报告

> **核查对象**：用户提出的产品能力描述——「AI 运维工单处理智能体：接入企业已有的工单系统 / IM / 监控告警产生的工单，AI 理解工单 → 检索上下文（知识库 / 历史工单 / 日志）→ 推荐处置方案 → 人工审批后执行修复 → 自动验证 → 沉淀经验 → 回写工单闭环」。
> **方法**：逐环节代码实测（2026-08-26），每项附代码位置或「全仓 0 命中」证据。
> **结论**：**8 步中 5 步完整实现、3 步有断点**——站内闭环已打通，断点集中在「外部接入」与「真实执行」两端。

---

## 一、逐环节核查结果

| # | 环节 | 状态 | 代码证据 |
|:---:|------|:---:|---------|
| 1a | 监控告警自动建单 | ✅ | `AlertWebhookController` + `AlertService.processWebhook`（13 处自动建单逻辑，开关可关，Single Writer 契约） |
| 1b | 外部工单系统接入（Jira/ServiceNow 等） | ❌ | 全仓 0 命中 |
| 1c | IM 入向建单（钉钉/企微发消息建单） | ❌ | 钉钉仅**出向通知**（`DingTalkNotifier`）；`TicketService` 中「机器人」仅为注释（群机器人 @所有人 会刷屏的设计取舍） |
| 1d | 站内建单（手动表单 + AI 对话建单） | ✅ | 双人工入口（PRD §四） |
| 2a | AI 理解工单（分析卡片） | ✅ | `TicketAiAnalysisService` + `/ai-analysis` 端点 + `AIContextPanel` 三模式 |
| 2b | 建单即自动分析 | ❌ | 无 `@EventListener` 自动触发，需人工点开分析 |
| 3a | 检索知识库 | ✅ | `HybridRetrieverService`（混合检索 + 0.73 熔断） |
| 3b | 检索历史工单 | ✅ | `TicketInsights.vue` similarTickets（同服务筛选）+ `fetchTickets` |
| 3c | 检索日志 | ❌ | 全仓无 SLS/ELK/Loki 接入 |
| 4 | 推荐处置方案 | ✅ | 结构化分析卡片（可能原因/排查命令/置信度/引用文档）+ 处置阶段机 + 动作列表 |
| 5a | 人工审批后执行（机制） | ✅ | `ApprovalController`（approve/reject）+ `ApprovalOrchestrator`（EXECUTED/EXECUTE_FAILED 状态机） |
| 5b | 真正执行修复（kubectl/ssh 真实动作） | ❌ | 执行器为零——审批通过的「执行」目前只有「创建工单」一个写操作 |
| 6a | 验证/跳过验证 | ✅ | `submitVerification` / `skipVerification` |
| 6b | 自动验证（监控指标自动确认恢复） | ❌ | 验证为人工点击，无监控数据自动校验 |
| 7 | 沉淀经验（复盘→知识库） | ✅ | `KnowledgeSinkDrawer` 一键转知识库（含来源回链）+ ActionItem |
| 8a | 站内工单闭环（状态机/活动流/时间线） | ✅ | 状态机 + activities + 关闭/作废 |
| 8b | 回写外部工单系统/IM | ❌ | 无出向回调（钉钉仅为通知，非回写） |

---

## 二、链路总览

```
监控告警 ──✅──▶ AI理解工单 ──✅──▶ 检索上下文(知识库✅ 历史工单✅ 日志❌)
   ▲                                           │
   │                                       推荐方案✅
   │                                           │
回写外部❌ ◀── 沉淀经验✅ ◀── 自动验证❌(人工✅) ◀── 执行修复❌(审批✅) ◀──┘
```

**已打通的（站内完整闭环）**：告警建单 → AI 分析 → 知识/历史工单检索 → 方案推荐 → 人审 →（执行=建单级）→ 人工验证 → 复盘沉淀 → 站内工单闭环。

**四个断点**：

| 断点 | 缺口 | 补法 | 排期 |
|------|------|------|------|
| A | 外部工单系统/IM 入向接入 | 通用入向适配器（工单来源抽象 `TicketSource` + Webhook 端点） | 新模块（本报告 §四） |
| B | 日志检索 | 阿里云 SLS 接入（蓝图契约已定 `/webhook/aliyun-sls`） | V2.0（PRD §16.1） |
| C | 真实执行修复 + 自动验证 | P4 执行器（V1.2）+ 验证改「人工提交 + 监控指标自动校验」双通道 | V1.2/V1.3 |
| D | 回写外部系统 | 出向 Webhook（处置结果回写源工单系统） | 依赖 A + C |

---

## 三、与现有 PRD 的关系

- **已覆盖**：1a、1d、2a、3a、3b、4、5a、6a、7、8a（站内闭环）——PRD v2.2 均已描述。
- **未覆盖**：1b、1c、8b（外部系统双向接入）、2b（自动分析）、6b（自动验证）。
- **缺口归属**：B/C 已在 PRD v2.2 路线图内；A/D（外部工单系统/IM 双向接入）**是新模块**，按用户确认补入 PRD v2.3（见 PRD §十九 + 附录 E）。
- **2b/6b 判断**：2b（建单自动分析）作为「L2 主动感知」的增强项，价值高（告警建单后立刻出分析），排入 V2.0；6b（自动验证）依赖执行器 + 监控数据，排入 V1.3 后。

---

## 四、新模块设计要点（已并入 PRD v2.3 §十九）

### 4.1 入向接入（工单来源适配器）

| 来源 | 接入方式 | 契约（对齐现有 `AlertmanagerWebhook` DTO 风格） | 优先级 |
|------|---------|---------------------------------------------|:---:|
| 外部工单系统（Jira/ServiceNow/自研） | 通用 Webhook `POST /api/v1/webhook/external-ticket` | `ExternalTicketEvent`：title/description/priority/module/source/externalId/callbackUrl | P1 |
| IM（钉钉/企微机器人） | IM 回调 Webhook `POST /api/v1/webhook/im` | 消息文本 → 解析建单；`source=IM` 留痕 | P1 |
| 监控告警（已有） | 现有 `AlertWebhookController` | 保持不动 | ✅ |

- **统一入口**：全部走 `TicketService.createTicket(title, priority, module, description, assignee, category, sla, creator, tags)`（已确认签名），来源经 `TicketSource` 枚举（INTERNAL / ALERT / EXTERNAL / IM）留痕。
- **幂等**：`externalId` 唯一索引，防重复建单（对齐告警 dedupKey 思路）。
- **映射**：外部系统优先级 → 站内四档（P0-P3）+ SLA 时限表映射。

### 4.2 出向回写（处置结果回写源系统）

| 触发点 | 回写内容 | 方式 |
|--------|---------|------|
| 工单状态变更（处理中/已解决/已关闭/作废） | 状态 + 处理摘要 + 根因 + 验证结果 + 知识库链接 | 出向 Webhook POST 到 `callbackUrl`（入向事件携带） |
| 审批结果（高危动作） | 审批人/时间/动作/结果 | 同上 |
| 验证通过/跳过 | 验证结论 | 同上 |

- **契约**：`TicketStatusCallback` JSON；失败重试 3 次 + 留痕待补发（对齐通知旁路契约）。
- **范围**：仅回写「来源为外部系统/IM」的工单，站内工单不回写。

### 4.3 关键设计取舍

- **不做 IM 全双向对话**（即 IM 里问答/审批），本期只做「IM 发消息建单 + 处置结果回写」；
- **钉钉一键授权按钮**（蓝图 P0/P1 路径）单列，不并入本模块；
- 自动分析（2b）作为增强项排 V2.0，不阻塞本模块验收。

---

## 五、建议执行顺序

```
V1.2（执行器）完成后 → 新模块立项（P1，约 1-2 周）：
  ① 入向 Webhook + ExternalTicketEvent + TicketSource 留痕（3-4 天）
  ② 幂等 externalId + 优先级/SLA 映射（1-2 天）
  ③ 出向回写 TicketStatusCallback + 重试（2-3 天）
  ④ 前端「来源」标识与外部链接展示（1-2 天）
  ⑤ 集成测试：Jira 模拟回调 → 建单 → 状态变更 → 回写（2 天）

前置：V1.1（Saga 前端 + 可观测性）→ V1.2（执行器）——本模块建议排在 V2.0 与执行器之间。
```

---

## 六、一句话总结

**「AI 运维工单处理智能体」的站内闭环已经完整（5/8 环节 ✅），缺口全部集中在「接入外部世界」：入向（外部工单/IM）与出向（回写）需要新增双向适配器模块，日志检索与真实执行已在既有路线图内。** 新增模块设计已并入 PRD v2.3 §十九。
