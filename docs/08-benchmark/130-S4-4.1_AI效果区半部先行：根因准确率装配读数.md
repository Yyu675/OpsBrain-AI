# 130 4-4.1 AI 效果区·半部先行：根因准确率装配读数

> 报告性质：路线图 §8.4（4-4.1）批次 27（T43）。
> 4-4.1 三项指标（根因准确率/幻觉率/证据不足率）里只有第一项不依赖
> EVAL_LLM 窗——它的数据源是**反馈闭环**（`submitAiAnalysisFeedback` +
> 后端 `GET /tickets/ai-analysis/stats`），全部在线服务。本批把这条
> 从批 22 存证的「成对半成面」接进看板：**后端零改动，纯读数装配**。

---

## 一、动线与口径

| 层 | 件 | 说明 |
|---|---|---|
| API 型 | `AiAnalysisStats` 恢复 `export` | 批 24 去化、批 27 真消费——**去化纪律的双向性范例**：去化不是永别，需求出现即加回 |
| queryKeys | `dashboardKeys.aiEffectStats()` | 入 dashboardKeys 楼（诊断/效果同属 AI 质量观） |
| hook | `useAiAnalysisStatsQuery()`（挂 dashboard.query.ts） | 注释立法：幻觉率/证据不足率持 LLM 窗数据再入区，**不发空壳 UI** |
| 前端 | B6 诊断区内「AI 根因分析反馈」三卡 | 根因准确率（rated=0→—）/ 有用·已评分 / 累计分析；六查询一并重拉（注释五→六） |
| 口径 | `rated===0 → '—'`，永不显示 `0.0%` | 「还没有任何人评过分」不是「准确率 0%」——Dashboard 页祖传纪律第四次执行 |

## 二、测试面（含一条乌龙案卷）

- smoke +2（合 30）：851/60 → `85.0%` 正例；`rated=0` → `—` + `暂无反馈` 反例。
- **乌龙案卷**：初版 `not.toContain('0.0%')` 全页断言失败——方向表的
  `100.0%` 天然含子串 `0.0%`。修成 `.ai-effect` 块级断言。「全页
  toContain 是子字符串陷阱的第一现场」补进该文件测试头注释的教训序列。

## 三、4-4 终幕牌面

| 件 | 状态 |
|---|---|
| 4-4.2 诊断区（点/链/时三轴） | ✅ 批 16/17 |
| 4-4.3 成本区 | ✅ 批 23（单次均价） |
| 4-4.1 根因准确率（反馈闭环） | ✅ **本批** |
| 4-4.1 幻觉率 / 证据不足率 | ⏳ 唯一锁：EVAL_LLM 本地窗首跑（D-B，你侧） |
| S4-2 校准 ECE | ⏳ 同锁 |

阶段 4 的 UI 骨架至此全部就位——窗数据一到，剩两个 KPI 卡的装配
是同楼同构半小时工。

## 四、验证

smoke 30/30、`vue-tsc` 0、全量 vitest 93 文件 **1808** 绿、build ✓、lint RC=0。
