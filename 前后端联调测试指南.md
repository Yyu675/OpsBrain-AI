# 前后端联调测试指南

## 测试时间
2026-07-17

## 前置准备

### 1. 启动后端服务
```bash
cd "D:\JAVA\Code_repository\OpsBrain AI\devops-platform-backend"

# 启动 Docker 环境（PostgreSQL + Redis）
docker compose -f docker-compose.dev.yml up -d

# 等待数据库就绪（约10秒）
sleep 10

# 执行数据库初始化脚本
docker exec -i devops-postgres psql -U devops -d devops_ai < sql/init.sql
docker exec -i devops-postgres psql -U devops -d devops_ai < sql/ticket_extensions.sql

# 启动后端服务
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

**验证后端启动成功**：
- 日志显示 `Started DevOpsPlatformBackendApplication`
- 访问 http://localhost:8088/ai 返回 API 信息

---

### 2. 启动前端服务
```bash
cd "D:\JAVA\Code_repository\OpsBrain AI\devops-platform-backend\devops-platform-frontend"

# 安装依赖（如果还没安装）
npm install

# 启动开发服务器
npm run dev
```

**验证前端启动成功**：
- 访问 http://localhost:5173
- 控制台无错误

---

## 测试场景

### 场景1：工单列表加载（核心功能）

**测试步骤**：
1. 打开浏览器访问 http://localhost:5173
2. 点击左侧导航栏"智能工单"
3. 观察工单列表是否正确加载

**预期结果**：
- ✅ 页面显示"正在加载..."状态
- ✅ 列表正确展示后端工单数据（如果数据库为空，显示"暂无工单"）
- ✅ 工单 ID 格式为 `TKT-yyyyMMdd-序号`（如 `TKT-20260717-0001`）
- ✅ 优先级、状态、负责人等字段正确显示

**检查点**：
- 浏览器开发者工具 Network 面板：
  - 请求 `GET http://localhost:8088/ai/api/v1/tickets?page=1&size=100`
  - 响应状态 200
  - 响应格式符合 `ApiResponse<TicketsResponse>`

**调试建议**：
- 如果 404：检查后端是否启动，TicketController 是否正确配置
- 如果 CORS 错误：检查后端 CORS 配置
- 如果列表为空：检查数据库是否有数据

---

### 场景2：AI 对话创建工单（核心功能）

**测试步骤**：
1. 访问首页 http://localhost:5173
2. 在 AI 对话框输入：
   ```
   我的 K8s Pod 一直 CrashLoopBackOff，容器启动后立即退出，请帮我开个工单
   ```
3. 观察 AI 响应
4. 切换到"智能工单"页面，刷新列表

**预期结果**：
- ✅ AI 识别需要创建工单
- ✅ AI 调用 `createDevOpsTicket` 工具
- ✅ 工具返回工单号（如 `TKT-20260717-0001`）
- ✅ AI 回复包含工单号和链接
- ✅ 工单列表中出现新创建的工单
- ✅ 工单字段正确：
  - title: 包含 "K8s" 或 "Pod" 关键词
  - priority: HIGH（因为是 CrashLoopBackOff）
  - module: K8S
  - category: 容器/K8s
  - status: PENDING
  - assignee: 待分配
  - sourceTraceId: 与对话 traceId 一致

**检查点**：
- 后端日志：
  ```
  🎫 [TicketService] 工单创建成功: TKT-20260717-0001 | 优先级: HIGH | 模块: K8S | 负责人: 待分配
  ```
- 数据库验证：
  ```sql
  SELECT * FROM sys_devops_ticket ORDER BY create_time DESC LIMIT 1;
  ```

**调试建议**：
- 如果 AI 没有调用工具：检查 LLM Prompt 和工具白名单
- 如果工具调用失败：检查 TicketService 逻辑和数据库连接
- 如果 sourceTraceId 为空：检查 TraceContext 是否正确设置

---

### 场景3：工单筛选和分页

**测试步骤**：
1. 在工单列表页面
2. 使用状态筛选器选择"待处理"
3. 使用优先级筛选器选择"高"
4. 在搜索框输入关键词
5. 点击分页按钮

**预期结果**：
- ✅ 筛选器正确过滤工单
- ✅ 搜索功能正确匹配工单标题/描述
- ✅ 分页正确切换
- ✅ 前端不会发送多余的 API 请求（因为数据已在本地）

**检查点**：
- 筛选后工单数量正确
- 分页计算正确

---

### 场景4：工单 ID 格式统一

**测试步骤**：
1. 创建多个工单（通过 AI 对话）
2. 观察工单 ID 格式
3. 检查工单详情页 URL

**预期结果**：
- ✅ 所有工单 ID 格式统一为 `TKT-yyyyMMdd-序号`
- ✅ 序号每日从 0001 开始递增
- ✅ 工单详情页 URL 使用后端 ID（如 `/tickets/TKT-20260717-0001`）

**检查点**：
- 前端不再显示 `OPS-2024-xxxx` 格式
- Redis 键正确生成：`devops:ticket:seq:20260717`

---

### 场景5：字段映射正确性

**测试步骤**：
1. 创建一个工单，module 为 MYSQL，priority 为 HIGH
2. 查看工单列表和详情

**预期结果**：
- ✅ 前端显示：
  - service: "生产环境-MySQL" 或显示 module 值
  - category: "数据库"（自动映射）
  - priority: "高"（枚举值正确转换）
  - sla: "4h 响应 / 8h 解决"（自动映射）
  - tags: 包含 "生产环境"、"MySQL"、"数据库"

**检查点**：
- 后端 DTO 转换正确
- 前端枚举映射正确

---

### 场景6：错误处理

**测试步骤**：
1. 停止后端服务
2. 前端刷新工单列表
3. 重新启动后端
4. 前端再次刷新

**预期结果**：
- ✅ 后端停止时：前端显示错误提示"加载工单列表失败"
- ✅ 后端恢复后：前端成功加载数据
- ✅ 前端不会崩溃

---

## 已知限制（计划中）

### 当前不支持的功能
1. **工单详情页回复和活动流** - 后端接口未实现
2. **手动创建工单表单** - 后端接口未实现
3. **工单状态更新** - 后端接口未实现
4. **工单分配人员** - 后端接口未实现
5. **附件上传** - 后端功能未实现

### 前端降级处理
- 工单详情页的 replies 和 activities 字段为空数组（前端兜底显示"暂无回复"）
- 工单操作按钮暂时禁用或隐藏

---

## 性能测试

### 并发创建工单
```bash
# 使用 ApacheBench 测试
ab -n 100 -c 10 -p payload.json -T application/json http://localhost:8088/ai/api/v1/tickets
```

**预期**：
- ✅ 工单序号连续无重复（Redis INCR 保证原子性）
- ✅ P99 延迟 < 2s
- ✅ 无数据库死锁或并发错误

---

## 测试检查清单

### 后端
- [ ] 编译通过（mvn clean compile）
- [ ] 数据库表结构正确（14 个字段 + 索引）
- [ ] TicketController 接口可访问
- [ ] Redis 序号生成正确
- [ ] traceId 传递链路完整
- [ ] 日志输出正确

### 前端
- [ ] 编译通过（npm run build 无错误）
- [ ] API 调用层正确封装
- [ ] DTO 转换正确
- [ ] store 从后端加载数据
- [ ] 工单列表正确显示
- [ ] 刷新按钮正常工作
- [ ] 错误提示正确显示

### 联调
- [ ] AI 对话创建工单成功
- [ ] 工单列表实时刷新
- [ ] 工单 ID 格式统一
- [ ] 字段映射正确
- [ ] 筛选和分页正常
- [ ] 错误处理正确

---

## 常见问题排查

### 问题1：前端请求后端 404
**原因**：后端未启动或 URL 配置错误

**解决**：
1. 检查后端是否启动：`curl http://localhost:8088/ai`
2. 检查前端 API 配置：`devops-platform-frontend/.env` 或 `src/api/config/api.ts`
3. 确认 baseURL 为 `http://localhost:8088/ai`

---

### 问题2：CORS 错误
**原因**：后端 CORS 配置缺失

**解决**：
后端添加 CORS 配置（如果还没有）：
```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("http://localhost:5173")
                .allowedMethods("*")
                .allowedHeaders("*");
    }
}
```

---

### 问题3：前端工单列表为空
**原因**：数据库无数据

**解决**：
1. 通过 AI 对话创建几个测试工单
2. 或者手动插入测试数据：
```sql
INSERT INTO sys_devops_ticket (id, title, priority, module, description, status, assignee, creator, category, sla, create_time, update_time)
VALUES ('TKT-20260717-0001', '测试工单', 'HIGH', 'K8S', '这是一个测试工单', 'PENDING', '待分配', 'devops-admin', '容器/K8s', '4h 响应 / 8h 解决', NOW(), NOW());
```

---

## 测试报告模板

```markdown
## 联调测试报告 - 2026-07-17

### 测试环境
- 后端：Spring Boot 3.x + JDK 21
- 前端：Vue 3 + Vite
- 数据库：PostgreSQL 15
- 缓存：Redis 7

### 测试结果
| 场景 | 状态 | 备注 |
|:---|:---:|:---|
| 工单列表加载 | ✅ | 正常 |
| AI 创建工单 | ✅ | 正常 |
| 工单筛选 | ✅ | 正常 |
| 工单分页 | ✅ | 正常 |
| ID 格式统一 | ✅ | 正常 |
| 字段映射 | ✅ | 正常 |
| 错误处理 | ✅ | 正常 |

### 发现的问题
1. （无）

### 待实现功能
1. 工单回复接口
2. 工单活动流接口
3. 手动创建工单接口
4. 工单状态更新接口

### 结论
✅ 核心功能联调通过，可进入下一阶段开发。
```

---

**准备就绪后，请按照测试步骤逐一验证！**
