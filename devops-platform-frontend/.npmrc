# @tanstack/vue-query 把 @vue/composition-api 声明为 peerOptional（Vue 2 兼容用），
# 而 npm 在解析 optional peer 时仍会尝试匹配版本，导致与本项目的 Vue 3.5 冲突：
#   peer vue@">= 2.5 < 2.7" from @vue/composition-api
# 实测该包并未被真正安装（node_modules/@vue/composition-api 不存在），
# 冲突纯属 npm 对 peerOptional 的解析行为。
#
# 不加此项则 `npm ci` / 他人首次 `npm install` 必然失败。
# 已核实：安装后依赖树中无 @vue/composition-api，vue 仍为单一 3.5.x 版本。
#
# ⚠️ 副作用：开启后 npm **不再自动安装 peerDependencies**。
# 实测已因此丢失 eslint-plugin-vue 的 peer `vue-eslint-parser`，导致 lint 直接崩。
# 因此：**新增任何依赖后，必须跑一遍 `npm run verify`**（typecheck + lint + test）；
# 若报「Cannot find module xxx」，把那个 peer 显式加进 devDependencies。
# 已显式声明的 peer：vue-eslint-parser（eslint-plugin-vue 所需）。
legacy-peer-deps=true
